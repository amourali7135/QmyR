<div class="row align-items-center justify-content-center py-1 ">
<h3>Show this QR to your new business connection</h3>
</div>
<div class="row align-items-center justify-content-center py-1 ">
<h3>Or</h3>
</div>
<div class="row align-items-center justify-content-center py-1 ">
<h3>Share this page with them: </h3>
</div>
<div class="row align-items-center justify-content-center py-1">
<%= social_share_button_tag(@business) %>
</div>

<%# qr = RQRCode::QRCode.new( 'my string to generate', :size => 4, :level => :h )
puts qr.to_s %>

<%# @qr = RQRCode::QRCode.new("http://localhost:3000/", size: 7, level: :h) %>
<%# conditional if logged in as user with profiles or not for auto add??
<% if Rails.env.production? %>
<div class="row align-items-center justify-content-center py-3">
<%= render_qr_code("http://qmyr.herokuapp.com/businesses/#{current_user.business.id}/") %>
<% else %>
<%= render_qr_code("http://localhost:3000/businesses/#{current_user.business.id}/") %>
</div>
<% end %>

<%#= qr = RQRCode::QRCode.new("http://localhost:3000/businesses/#{current_user.business.id}/") %>

